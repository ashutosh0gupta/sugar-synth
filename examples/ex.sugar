# input file format
(number-of-rules-to-learn 3)
(max-rule-depth 3)
(max-compartments 2)
(sugar A 2)
(sugar D 0)
(seed-mol A)
# (sugar B 2)
# (sugar Ca 3)
# (sugar Das 4)
#(mol (A (A (B _ _ ) Das) (Ca _ Das _ )) )
# (mol (A A _) )
# (mol (A (A D A) D) )
# (mol (A (A _ A) _) )
(mol (A (A A _) _) )
# (mol (B (B B A) A) )
# (mol (A (A _ A) A) )
# (mol (A _ (A _ A)) )
# (mol (A A (A _ A)) )
# (mol (A (A _ A) (A _ A)) )
# (mol (A (A A A) (A _ A)) )
# (mol (A _ (A A A)) )
# (mol (A B A) )